// Auto-generated by file split — part of the D&D 5e Character Sheet project
// Edit this file directly to add or modify spell data (lists, descriptions, damage, levels).

const CUSTOM_SPELL = '__custom__';

const SPELL_LISTS = {
  artificer: {
    0: ['Acid Splash','Dancing Lights','Fire Bolt','Guidance','Light','Mage Hand','Mending','Message','Poison Spray','Prestidigitation','Ray of Frost','Resistance','Shocking Grasp','Spare the Dying','Thorn Whip'],
    1: ['Alarm','Arcane Weapon','Cure Wounds','Detect Magic','Disguise Self','Expeditious Retreat','False Life','Grease','Identify','Jump','Longstrider','Sanctuary','Shield of Faith'],
    2: ['Aid','Alter Self','Arcane Lock','Blur','Continual Flame','Darkvision','Enhance Ability','Enlarge/Reduce','Heat Metal','Invisibility','Lesser Restoration','Levitate','Magic Mouth','Magic Weapon','Protection from Poison','Rope Trick','See Invisibility','Spider Climb'],
    3: ['Blink','Dispel Magic','Elemental Weapon','Fly','Gaseous Form','Glyph of Warding','Haste','Protection from Energy','Revivify','Water Breathing','Water Walk'],
    4: ['Arcane Eye','Fabricate','Freedom of Movement',"Leomund's Secret Chest","Mordenkainen's Faithful Hound","Mordenkainen's Private Sanctum","Otiluke's Resilient Sphere",'Stone Shape','Stoneskin'],
    5: ["Animate Objects","Bigby's Hand",'Creation','Greater Restoration','Wall of Stone']
  }
};

const SPELL_DESCRIPTIONS = {
  'Acid Splash': "Ranged acid spray; one or two creatures within 5 ft of each other take acid damage on a failed Dex save.",
  'Dancing Lights': "Create up to four torch-sized lights (or one dim humanoid shape) that you move around within range.",
  'Fire Bolt': "Ranged spell attack that deals fire damage; scales with an extra die at higher character levels.",
  'Guidance': "Touch a willing creature; it adds 1d4 to one ability check of its choice before the spell ends.",
  'Light': "Touch an object to make it shed bright light in a 20-ft radius for 1 hour.",
  'Mage Hand': "Conjure a spectral hand that can manipulate, carry, or retrieve small objects at range.",
  'Mending': "Repair a single break or tear in an object with a touch, no trace of the mend remaining.",
  'Message': "Whisper a message only one chosen creature within range can hear, with a short private reply.",
  'Poison Spray': "Project poison at a creature within 10 ft; poison damage on a failed Con save.",
  'Prestidigitation': "Minor magical trick: small sensory effects, cleaning/soiling, minor flavors, tiny objects, etc.",
  'Ray of Frost': "Ranged spell attack dealing cold damage and reducing the target's speed by 10 ft until your next turn.",
  'Resistance': "Touch a willing creature to grant advantage on its next save against one chosen damage type.",
  'Shocking Grasp': "Melee spell attack dealing lightning damage; the target can't take reactions until its next turn.",
  'Spare the Dying': "Touch a creature at 0 HP to stabilize it without needing a death save.",
  'Thorn Whip': "Melee spell attack that deals piercing damage and pulls the target up to 10 ft closer to you.",

  'Alarm': "Ward an area; you're alerted (mentally or audibly) if a creature enters it uninvited within 8 hours.",
  'Arcane Weapon': "Turn a weapon you're holding into a magic weapon that deals extra force damage for the duration.",
  'Cure Wounds': "Touch a creature to restore hit points; scales with a higher-level slot.",
  'Detect Magic': "Sense the presence of magic within 30 ft and see faint auras around magical things.",
  'Disguise Self': "Change your appearance (height, features, clothing) until the spell ends or you dismiss it.",
  'Expeditious Retreat': "Bonus action Dash on your turns for the duration; great for repositioning.",
  'False Life': "Gain temporary hit points for the duration.",
  'Grease': "Cover a 10-ft square in slick grease; creatures there must save or fall prone.",
  'Identify': "Learn the properties of a magic item, or the spells affecting a creature/object, over 1 minute.",
  'Jump': "Touch a creature to triple its jump distance for the duration.",
  'Longstrider': "Touch a creature to increase its speed by 10 ft for the duration.",
  'Sanctuary': "Ward a creature so attackers must make a Wisdom save or redirect their attack elsewhere.",
  'Shield of Faith': "A shimmering field grants a targeted creature +2 AC for the duration.",

  'Aid': "Bolster up to three allies, increasing their max and current HP for the duration.",
  'Alter Self': "Change your own form: aquatic adaptation, a natural weapon, or a disguised appearance.",
  'Arcane Lock': "Magically lock a door, chest, or portal so only you or those you designate can open it.",
  'Blur': "Your form flickers, giving attackers disadvantage on attack rolls against you.",
  'Continual Flame': "Create a flameless, heatless magical flame that burns indefinitely on an object.",
  'Darkvision': "Touch a creature to grant it darkvision out to 60 ft for the duration.",
  'Enhance Ability': "Grant a creature advantage on checks of one chosen ability, plus a minor extra benefit.",
  'Enlarge/Reduce': "Cause a creature or object to grow larger (bigger, stronger) or shrink (smaller, weaker).",
  'Heat Metal': "Make a metal object glow hot, damaging whoever's holding/wearing it; repeatable each turn.",
  'Invisibility': "Make a willing creature invisible until the spell ends, attacking, or casting a spell.",
  'Lesser Restoration': "Touch a creature to end one disease or condition (blinded, paralyzed, poisoned, etc.).",
  'Levitate': "Make a creature or object float up/down and move vertically for the duration.",
  'Magic Mouth': "Implant a message that an object or creature speaks when a trigger condition you set occurs.",
  'Magic Weapon': "Touch a nonmagical weapon to make it magical, granting a +1 bonus to attack and damage rolls.",
  'Protection from Poison': "Touch a creature to neutralize one poison, cure poisoned condition, and grant resistance.",
  'Rope Trick': "An invisible extradimensional space opens at the top of a rope, hiding up to 8 creatures.",
  'See Invisibility': "See invisible creatures/objects and see into the Ethereal Plane for the duration.",
  'Spider Climb': "Touch a creature to let it move on walls/ceilings and gain a climbing speed.",

  'Blink': "Roll each turn a chance to slip briefly to the Ethereal Plane, becoming hard to hit or reach.",
  'Dispel Magic': "End one spell affecting a creature, object, or effect within range.",
  'Elemental Weapon': "Imbue a weapon with elemental energy, adding bonus damage of a chosen type.",
  'Fly': "Touch a creature to grant it a flying speed of 60 ft for the duration.",
  'Gaseous Form': "Turn a willing creature into a misty cloud, immune to most damage and able to squeeze through gaps.",
  'Glyph of Warding': "Inscribe a glyph that triggers a spell or explosion when its trigger condition is met.",
  'Haste': "Double a creature's speed, +2 AC, advantage on Dex saves, and an extra action (minus a second attack) — but it's slowed when the spell ends.",
  'Protection from Energy': "Touch a creature to grant resistance to one damage type (acid, cold, fire, lightning, thunder).",
  'Revivify': "Touch a creature dead for no more than a minute to return it to life with 1 HP.",
  'Water Breathing': "Grant up to 10 creatures the ability to breathe underwater for 24 hours.",
  'Water Walk': "Grant up to 10 creatures the ability to move across any liquid surface as if it were solid ground.",

  'Arcane Eye': "Create an invisible, magical eye that flies around and lets you see through it remotely.",
  'Fabricate': "Convert raw materials into a finished product of the same material, instantly.",
  'Freedom of Movement': "Touch a creature so its movement is unaffected by difficult terrain, restraint, or paralysis effects.",
  "Leomund's Secret Chest": "Hide an ornate chest on the Ethereal Plane, recallable at will; the linked replica lets you access it.",
  "Mordenkainen's Faithful Hound": "Conjure an invisible watchdog that barks at intruders and can bite an enemy each round.",
  "Mordenkainen's Private Sanctum": "Ward an area against scrying, teleportation, and sound/light passing through its boundary.",
  "Otiluke's Resilient Sphere": "Encase a creature or object in an impenetrable, movable magical sphere.",
  'Stone Shape': "Reshape a section of stone into any form useful to you (a door, weapon, statue, etc.).",
  'Stoneskin': "Touch a creature to grant resistance to nonmagical bludgeoning, piercing, and slashing damage.",

  'Animate Objects': "Bring up to ten small (or fewer, larger) nonmagical objects to life to fight for you.",
  "Bigby's Hand": "Create a spectral hand that can push, grapple, block, or strike as you direct it each turn.",
  'Creation': "Fabricate an object of nonliving matter from raw materials shaped by light and shadow.",
  'Greater Restoration': "Touch a creature to remove exhaustion, restore a reduced ability score, or end curses/petrification.",
  'Wall of Stone': "Create a nonmagical wall of solid stone in a shape of your choosing.",

  // --- Simic Guild Spells (background) ---
  'Druidcraft': "A tiny bit of nature magic: predict weather, make a flower bloom, or create a small sensory effect.",
  'Detect Poison and Disease': "Sense the presence and location of poison, poisonous creatures, and disease within 30 ft.",

  // --- Subclass bonus spells (Alchemist / Armorer / Artillerist / Battle Smith) ---
  'Healing Word': "Bonus-action ranged heal; restore hit points to a creature within 60 ft without touching it.",
  'Ray of Sickness': "Ranged spell attack dealing poison damage; on a hit the target is also poisoned until your next turn.",
  'Flaming Sphere': "Conjure a rolling 5-ft sphere of fire that burns nearby creatures and that you can move each turn.",
  "Melf's Acid Arrow": "Ranged spell attack dealing acid damage now, with more acid damage at the end of the target's next turn.",
  'Mass Healing Word': "Bonus-action heal up to six creatures within 60 ft at once.",
  'Blight': "Necrotic energy withers a target (or a plant massively) for heavy damage on a failed Con save.",
  'Death Ward': "Touch a creature so the first time it would drop to 0 HP, it instead drops to 1 HP.",
  'Cloudkill': "A 20-ft sphere of poisonous fog that damages and can kill creatures caught inside each turn.",
  'Raise Dead': "Return a dead creature to life (if willing) as long as its body is mostly intact and death wasn't too long ago.",
  'Magic Missile': "Three automatically-hitting darts of force damage that you can split between targets.",
  'Mirror Image': "Create three illusory duplicates of yourself that can be struck instead of you.",
  'Hypnotic Pattern': "A dazzling area pattern that can leave several creatures charmed and incapacitated.",
  'Lightning Bolt': "A 100-ft line of lightning damages everything caught along its length on a failed Dex save.",
  'Passwall': "Create a temporary passage through a wall, floor, or ceiling of wood, plaster, or stone.",
  'Shield': "Reaction spell granting +5 AC and immunity to magic missile until your next turn.",
  'Thunderwave': "A wave of thunderous force pushes creatures back and damages them in front of you.",
  'Scorching Ray': "Hurl three separate rays of fire, each a ranged spell attack dealing fire damage.",
  'Shatter': "A loud ringing noise damages everything in a 10-ft sphere, extra harsh on inorganic material.",
  'Fireball': "A classic: a 20-ft ball of fire deals heavy damage to everything caught in the area.",
  'Wind Wall': "A wall of strong wind deflects small objects, arrows, and gas, and halves damage from ranged attacks.",
  'Ice Storm': "Hail and ice pummel a 20-ft cylinder, dealing bludgeoning and cold damage to everyone inside.",
  'Wall of Fire': "Create a wall of fire that damages anyone passing through or standing within 10 ft.",
  'Cone of Cold': "A blast of frigid air in a 60-ft cone deals heavy cold damage to everything inside.",
  'Heroism': "Touch a willing creature to grant it immunity to fear and temporary HP each turn for the duration.",
  'Branding Smite': "Your next weapon hit deals extra radiant damage and makes the target visible/glowing.",
  'Warding Bond': "Link yourself to another creature, sharing your defenses and resistances at a cost to your own HP.",
  'Aura of Vitality': "Create a healing aura around yourself; spend actions each turn to heal allies who pass through it.",
  'Conjure Barrage': "Hurl a barrage of weapons or ammunition in a 60-ft cone, damaging everyone caught inside.",
  'Aura of Purity': "An aura that reduces poison/disease susceptibility and eases exhaustion, blinded, deafened, or paralyzed conditions for allies nearby.",
  'Banishing Smite': "Your next weapon hit deals extra force damage and can banish the target to another plane briefly.",
  'Mass Cure Wounds': "Heal a burst of up to six creatures in a 30-ft sphere at once.",
  'Polymorph': "Transform a creature into a different beast form, changing its statistics until the spell ends.",

  // --- Not on the standard Artificer list (Warlock/Wizard) — supported here for multiclass/feat/DM-approved use ---
  'Eldritch Blast': "A beam of crackling energy per target in range; you get more beams at higher character levels. Not on the standard Artificer list — Warlock cantrip.",
  "Hunter's Mark": "Mark a creature you can see; your weapon attacks against it deal extra damage, and you have advantage on tracking it. Concentration, 1 hour. Not on the standard Artificer list — Ranger spell.",
  'Find Familiar': "Ritual-cast a Tiny spirit in an animal form to scout, deliver touch spells, and keep you company. Not on the standard Artificer list — Wizard spell."
};

const SPELL_LEVELS = {
  'Healing Word':1,'Ray of Sickness':1,'Flaming Sphere':2,"Melf's Acid Arrow":2,'Mass Healing Word':3,
  'Blight':4,'Death Ward':4,'Cloudkill':5,'Raise Dead':5,'Magic Missile':1,'Mirror Image':2,
  'Hypnotic Pattern':3,'Lightning Bolt':3,'Fire Shield':4,'Greater Invisibility':4,'Passwall':5,'Wall of Force':5,
  'Shield':1,'Thunderwave':1,'Scorching Ray':2,'Shatter':2,'Fireball':3,'Wind Wall':3,'Ice Storm':4,'Wall of Fire':4,
  'Cone of Cold':5,'Heroism':1,'Branding Smite':2,'Warding Bond':2,'Aura of Vitality':3,'Conjure Barrage':3,
  'Aura of Purity':4,'Banishing Smite':5,'Mass Cure Wounds':5,'Druidcraft':0,'Detect Poison and Disease':1,'Polymorph':4,
  'Eldritch Blast':0,'Find Familiar':1,"Hunter's Mark":1
};

const SPELL_DAMAGE = {
  'Fire Bolt': {die:'d10', type:'fire', attack:true, cantripScales:true},
  'Acid Splash': {die:'d6', type:'acid', save:'Dex', cantripScales:true},
  'Poison Spray': {die:'d12', type:'poison', save:'Con', cantripScales:true},
  'Ray of Frost': {die:'d8', type:'cold', attack:true, cantripScales:true},
  'Shocking Grasp': {die:'d8', type:'lightning', attack:true, cantripScales:true},
  'Thorn Whip': {die:'d6', type:'piercing', attack:true, cantripScales:true},
  "Melf's Acid Arrow": {dice:'2d4', type:'acid', attack:true, note:"+1d4 acid at the end of the target's next turn"},
  'Flaming Sphere': {dice:'2d6', type:'fire', save:'Dex'},
  'Ray of Sickness': {dice:'2d8', type:'poison', attack:true},
  'Scorching Ray': {dice:'2d6', type:'fire', attack:true, note:'per ray, 3 rays'},
  'Fireball': {dice:'8d6', type:'fire', save:'Dex'},
  'Lightning Bolt': {dice:'8d6', type:'lightning', save:'Dex'},
  'Shatter': {dice:'3d8', type:'thunder', save:'Con'},
  'Thunderwave': {dice:'2d8', type:'thunder', save:'Con'},
  'Ice Storm': {dice:'2d8+4d6', type:'bludgeoning+cold', save:'Dex'},
  'Wall of Fire': {dice:'5d8', type:'fire', save:'Dex'},
  'Cone of Cold': {dice:'8d8', type:'cold', save:'Con'},
  'Magic Missile': {dice:'3×(1d4+1)', type:'force', note:'automatic hit, no roll needed'},
  'Blight': {dice:'8d6', type:'necrotic', save:'Con'},
  'Cloudkill': {dice:'5d8', type:'poison', save:'Con'},
  'Conjure Barrage': {dice:'3d8', type:'weapon-based', save:'Dex'},
  'Banishing Smite': {dice:'5d10', type:'force', note:'on a weapon hit'},
  'Branding Smite': {dice:'2d6', type:'radiant', note:'on a weapon hit'},
  'Eldritch Blast': {die:'d10', type:'force', attack:true, cantripScales:'beams'},
  "Hunter's Mark": {dice:'1d6', type:'(matches your weapon)', note:'Extra damage added on a weapon hit against the marked target — roll alongside your normal attack.'}
};

// ============================================================================
// Spellbook sidebar + Roll/Details modal
// Self-contained here so spell UI changes don't require touching js/app.js —
// it only reads globals app.js already exposes (ACTIVE, CUSTOM_SPELL,
// showToast, cantripDieCount) and DOM fields app.js already creates
// (slotTotal_N / slotExpended_N, .spell-level / .spell-name-select /
// .spell-prep on each spells-table row) at call time, after the page loads.
// ============================================================================

(function(){

  // ---------------------------------------------------------------------
  // Dice rolling
  // ---------------------------------------------------------------------
  function _d(nSides){ return 1 + Math.floor(Math.random() * nSides); }

  // Expands "3×(1d4+1)" style multiplier notation into "1d4+1+1d4+1+1d4+1"
  function _expandMultiplier(expr){
    return expr.replace(/(\d+)\s*[×x]\s*\(([^)]+)\)/gi, (m, n, inner) => {
      const count = parseInt(n, 10);
      return Array(count).fill(inner).join('+');
    });
  }

  // Rolls a dice expression like "2d8+4d6", "5d10", or "3×(1d4+1)".
  function rollDiceExpression(expr){
    if(!expr) return null;
    const expanded = _expandMultiplier(expr);
    const terms = expanded.match(/[+-]?[^+-]+/g) || [];
    let total = 0;
    const parts = [];
    terms.forEach(raw=>{
      const trimmed = raw.trim();
      const sign = trimmed.startsWith('-') ? -1 : 1;
      const term = trimmed.replace(/^[+-]/, '').trim();
      const diceMatch = term.match(/^(\d+)d(\d+)$/i);
      if(diceMatch){
        const n = parseInt(diceMatch[1], 10);
        const s = parseInt(diceMatch[2], 10);
        const rolls = [];
        for(let i=0;i<n;i++) rolls.push(_d(s));
        const sum = rolls.reduce((a,b)=>a+b, 0);
        total += sign * sum;
        parts.push(`${sign<0?'-':''}${n}d${s} [${rolls.join('+')}]`);
      } else if(/^\d+$/.test(term)){
        const val = parseInt(term, 10);
        total += sign * val;
        parts.push(`${sign<0?'-':'+'}${val}`);
      } else if(term){
        parts.push(term);
      }
    });
    return { total, breakdown: parts.join(' ') };
  }

  // ---------------------------------------------------------------------
  // Reading current character state
  // ---------------------------------------------------------------------
  function _charLevel(){
    try { if(typeof ACTIVE !== 'undefined' && ACTIVE && ACTIVE.level) return ACTIVE.level; } catch(e){}
    return 1;
  }
  function _cantripCount(level){
    try { if(typeof cantripDieCount === 'function') return cantripDieCount(level); } catch(e){}
    if(level>=17) return 4;
    if(level>=11) return 3;
    if(level>=5) return 2;
    return 1;
  }
  // Parses the sheet's computed "DC 15 / +7 · prepare 5" display, if present.
  function _spellDcAtk(){
    const el = document.getElementById('spellDcAtk');
    if(!el) return { dc:null, atk:null };
    const m = el.textContent.match(/DC\s*(-?\d+)\s*\/\s*([+-]?\d+)/);
    if(!m) return { dc:null, atk:null };
    return { dc: parseInt(m[1],10), atk: parseInt(m[2],10) };
  }

  // ---------------------------------------------------------------------
  // Roll-relevant info for a spell, from SPELL_DAMAGE
  // ---------------------------------------------------------------------
  function getSpellRollInfo(name, level){
    const d = (typeof SPELL_DAMAGE !== 'undefined') ? SPELL_DAMAGE[name] : null;
    if(!d) return null;
    let expr;
    if(d.cantripScales === 'beams'){
      const count = _cantripCount(level || _charLevel());
      return { beams: count, die: d.die, type: d.type, attack: true, note: d.note };
    }
    if(d.cantripScales){
      const count = _cantripCount(level || _charLevel());
      const sides = (d.die || '').replace(/^d/i, '');
      expr = `${count}d${sides}`;
    } else {
      expr = d.dice || (d.die ? `1${d.die}` : null);
    }
    return { expr, type: d.type, attack: d.attack||false, save: d.save||null, note: d.note||null };
  }

  // Quick badge for the sidebar: "🎯+8" / "🛡️DC16" / "" 
  function getSpellBadge(name){
    const d = (typeof SPELL_DAMAGE !== 'undefined') ? SPELL_DAMAGE[name] : null;
    if(!d) return '';
    const { dc, atk } = _spellDcAtk();
    if(d.attack || d.cantripScales === 'beams') return atk!==null ? `🎯 ${atk>=0?'+':''}${atk}` : '🎯 —';
    if(d.save) return dc!==null ? `🛡️ DC ${dc}` : '🛡️ DC —';
    return '🎲';
  }

  // ---------------------------------------------------------------------
  // Modal content builders
  // ---------------------------------------------------------------------
  function buildRollModalContent(name, level){
    const info = getSpellRollInfo(name, level);
    if(!info){
      return `<div class="desc-text">This spell doesn't have an automatic dice roll set up — check <strong>Details</strong> for what it does, or roll manually per its description.</div>`;
    }
    let html = '';
    const { dc, atk } = _spellDcAtk();

    if(info.beams){
      for(let i=1;i<=info.beams;i++){
        const atkRoll = _d(20);
        const atkTotal = (atk!==null) ? atkRoll + atk : atkRoll;
        const dmgRoll = _d(parseInt((info.die||'d10').replace(/^d/i,''),10));
        html += `<div class="roll-line">Beam ${i} — Attack: ${atk!==null?`${atkTotal} (d20: ${atkRoll} + ${atk})`:`${atkRoll} (raw d20 — set your Spellcasting Ability to compute the bonus)`}<br>Damage: <span class="roll-total">${dmgRoll}</span> ${info.type||''}</div>`;
      }
      return html + `<div class="roll-note">Each beam can target a different creature.</div>`;
    }

    if(info.attack){
      const atkRoll = _d(20);
      const atkTotal = (atk!==null) ? atkRoll + atk : atkRoll;
      html += `<div class="roll-line">Attack roll: <span class="roll-total">${atkTotal}</span> ${atk!==null?`(d20: ${atkRoll} + ${atk})`:'(raw d20 — set your Spellcasting Ability to compute the bonus)'}<br><span class="roll-note" style="margin-top:0;">On a hit vs. the target's AC, roll damage below.</span></div>`;
    } else if(info.save){
      html += `<div class="roll-line">Target makes a <strong>${info.save} saving throw</strong>${dc!==null?` (DC ${dc})`:''}.<br><span class="roll-note" style="margin-top:0;">Fail: takes full damage below. Success: takes half (round down), unless the spell says otherwise.</span></div>`;
    } else if(info.note){
      html += `<div class="roll-line">${info.note}</div>`;
    }

    if(info.expr){
      const result = rollDiceExpression(info.expr);
      if(result){
        html += `<div class="roll-line">Damage (${info.expr}${info.type?`, ${info.type}`:''}): <span class="roll-total">${result.total}</span><br><span class="roll-note" style="margin-top:0;">${result.breakdown}</span></div>`;
      }
    }
    return html || `<div class="desc-text">No automatic roll for this spell — check Details.</div>`;
  }

  function buildDetailsModalContent(name, level){
    const desc = (typeof SPELL_DESCRIPTIONS !== 'undefined') ? SPELL_DESCRIPTIONS[name] : '';
    const effect = (typeof spellEffectText === 'function') ? spellEffectText(name, level || _charLevel()) : '';
    let html = '';
    html += `<div class="desc-text">${desc || 'No description on file for this spell yet — you can still use Roll if it has damage data.'}</div>`;
    if(effect) html += `<div class="roll-line">${effect}</div>`;
    return html;
  }

  // ---------------------------------------------------------------------
  // Modal open/close
  // ---------------------------------------------------------------------
  function ensureModalWired(){
    const overlay = document.getElementById('spellModalOverlay');
    const closeBtn = document.getElementById('spellModalClose');
    if(!overlay || overlay._wired) return;
    overlay._wired = true;
    closeBtn.addEventListener('click', closeSpellModal);
    overlay.addEventListener('click', (e)=>{ if(e.target === overlay) closeSpellModal(); });
    document.addEventListener('keydown', (e)=>{ if(e.key === 'Escape') closeSpellModal(); });
  }
  function openSpellModal(kicker, title, bodyHtml){
    ensureModalWired();
    const overlay = document.getElementById('spellModalOverlay');
    document.getElementById('spellModalKicker').textContent = kicker;
    document.getElementById('spellModalTitle').textContent = title;
    document.getElementById('spellModalBody').innerHTML = bodyHtml;
    overlay.style.display = 'flex';
  }
  function closeSpellModal(){
    const overlay = document.getElementById('spellModalOverlay');
    if(overlay) overlay.style.display = 'none';
  }

  // ---------------------------------------------------------------------
  // Reading each underlying spells-table row (the existing editor table
  // remains the single source of truth — the sidebar is just a nicer view
  // onto the same rows, so nothing about save/load or choice-syncing changes)
  // ---------------------------------------------------------------------
  function _rowData(tr){
    const nameSel = tr.querySelector('.spell-name-select');
    const customInput = tr.querySelector('.spell-name-custom');
    const levelSel = tr.querySelector('.spell-level');
    const prepEl = tr.querySelector('.spell-prep');
    const customVal = (typeof CUSTOM_SPELL !== 'undefined') ? CUSTOM_SPELL : '__custom__';
    const name = nameSel && nameSel.value === customVal
      ? (customInput ? customInput.value.trim() : '')
      : (nameSel ? nameSel.value : '');
    const level = levelSel ? parseInt(levelSel.value, 10) || 0 : 0;
    const prepared = prepEl ? prepEl.checked : false;
    return { name, level, prepared };
  }
  function getAllSpellRows(){
    const body = document.querySelector('#spellsTable tbody');
    if(!body) return [];
    return [...body.querySelectorAll('tr')].map(_rowData).filter(r => r.name);
  }

  // ---------------------------------------------------------------------
  // Slot tracking (reads/writes the sheet's existing slotTotal_N /
  // slotExpended_N inputs — already created by app.js for levels 1-9)
  // ---------------------------------------------------------------------
  function _slotFields(level){
    return {
      total: document.querySelector(`[name="slotTotal_${level}"]`),
      used: document.querySelector(`[name="slotExpended_${level}"]`)
    };
  }
  function _slotCounts(level){
    const { total, used } = _slotFields(level);
    const t = total ? (parseInt(total.value, 10) || 0) : 0;
    const u = used ? (parseInt(used.value, 10) || 0) : 0;
    return { total: t, used: u };
  }
  function consumeSlot(level){
    if(!level) return; // cantrips never consume a slot
    const { used } = _slotFields(level);
    if(!used) return;
    const current = parseInt(used.value, 10) || 0;
    used.value = String(current + 1);
    used.dispatchEvent(new Event('change', { bubbles: true }));
    renderSpellbookSlots();
    if(typeof showToast === 'function'){
      const { total } = _slotCounts(level);
      showToast(`Level ${level} slot used (${current+1}/${total || '?'})`);
    }
  }
  function freeSlot(level){
    const { used } = _slotFields(level);
    if(!used) return;
    const current = parseInt(used.value, 10) || 0;
    used.value = String(Math.max(0, current - 1));
    used.dispatchEvent(new Event('change', { bubbles: true }));
    renderSpellbookSlots();
  }

  function renderSpellbookSlots(){
    const container = document.getElementById('spellbookSlots');
    if(!container) return;
    const rows = getAllSpellRows();
    const levelsInUse = new Set(rows.map(r => r.level).filter(l => l > 0));
    // also show any level that has a Total set, even with no spells listed yet
    for(let l=1;l<=9;l++){
      const { total } = _slotCounts(l);
      if(total > 0) levelsInUse.add(l);
    }
    const levels = [...levelsInUse].sort((a,b)=>a-b);
    if(!levels.length){
      container.innerHTML = `<span style="font-size:11.5px; color:var(--ink-soft);">No leveled slots set yet — fill in Slots Total on the sheet.</span>`;
      return;
    }
    container.innerHTML = levels.map(l=>{
      const { total, used } = _slotCounts(l);
      const remaining = Math.max(0, total - used);
      return `<span class="spellbook-slot-chip" data-level="${l}">
        Lv${l}: <strong>${remaining}/${total}</strong>
        <button type="button" class="sb-slot-free" title="Free a slot">−</button>
        <button type="button" class="sb-slot-use" title="Use a slot">+</button>
      </span>`;
    }).join('');
    container.querySelectorAll('.sb-slot-use').forEach(btn=>{
      btn.addEventListener('click', ()=> consumeSlot(parseInt(btn.closest('.spellbook-slot-chip').dataset.level,10)));
    });
    container.querySelectorAll('.sb-slot-free').forEach(btn=>{
      btn.addEventListener('click', ()=> freeSlot(parseInt(btn.closest('.spellbook-slot-chip').dataset.level,10)));
    });
  }

  // ---------------------------------------------------------------------
  // Spellbook list rendering
  // ---------------------------------------------------------------------
  function levelLabel(l){ return l === 0 ? 'Cantrips' : `Level ${l}`; }

  function renderSpellbookList(){
    const container = document.getElementById('spellbookList');
    if(!container) return;
    const rows = getAllSpellRows();
    if(!rows.length){
      container.innerHTML = `<div class="spellbook-empty">No spells added yet — add some in the Spellcasting section, then open the Spellbook to use them here.</div>`;
      return;
    }
    const byLevel = {};
    rows.forEach(r=>{ (byLevel[r.level] = byLevel[r.level] || []).push(r); });
    const levels = Object.keys(byLevel).map(Number).sort((a,b)=>a-b);
    container.innerHTML = levels.map(l=>{
      const items = byLevel[l].slice().sort((a,b)=> a.name.localeCompare(b.name));
      const itemsHtml = items.map(r=>{
        const badge = getSpellBadge(r.name);
        return `<div class="spellbook-item" data-name="${r.name.replace(/"/g,'&quot;')}" data-level="${r.level}">
          <div class="sb-name"><span class="sb-title${r.prepared?' prepared':''}">${r.name}</span></div>
          ${badge?`<span class="sb-badge">${badge}</span>`:''}
          <button type="button" class="sb-roll" title="Roll">🎲</button>
        </div>`;
      }).join('');
      return `<div class="spellbook-level-heading">${levelLabel(l)}</div>${itemsHtml}`;
    }).join('');

    container.querySelectorAll('.spellbook-item').forEach(item=>{
      const name = item.dataset.name;
      const level = parseInt(item.dataset.level, 10) || 0;
      item.querySelector('.sb-name').addEventListener('click', ()=>{
        openSpellModal('Details', name, buildDetailsModalContent(name, level));
      });
      item.querySelector('.sb-roll').addEventListener('click', (e)=>{
        e.stopPropagation();
        openSpellModal('Roll', name, buildRollModalContent(name, level));
        if(level > 0) consumeSlot(level);
      });
    });
  }

  function refreshSpellbook(){
    renderSpellbookSlots();
    renderSpellbookList();
  }

  // ---------------------------------------------------------------------
  // Sidebar open/close + keeping it in sync with the underlying table
  // ---------------------------------------------------------------------
  function openSpellbook(){
    refreshSpellbook();
    document.getElementById('spellbookOverlay').style.display = 'flex';
  }
  function closeSpellbook(){
    document.getElementById('spellbookOverlay').style.display = 'none';
  }

  // ---------------------------------------------------------------------
  // Rests — 5e rules: a Long Rest restores every spell slot and every
  // limited-use feature. A Short Rest only restores slots for classes
  // with Pact Magic (Warlock), and only restores limited-use features
  // explicitly marked "Short Rest" recharge.
  // ---------------------------------------------------------------------
  function _resetActionUses(rechargeTypes){
    let count = 0;
    ['#actionsTable', '#bonusActionsTable'].forEach(sel=>{
      document.querySelectorAll(`${sel} tbody tr`).forEach(tr=>{
        const limited = tr.querySelector('.act-limited');
        const rechargeSel = tr.querySelector('.act-recharge');
        if(!limited || !limited.checked || !rechargeSel) return;
        if(!rechargeTypes.includes(rechargeSel.value)) return;
        const maxEl = tr.querySelector('.act-uses-max');
        const curEl = tr.querySelector('.act-uses-current');
        if(maxEl && curEl && curEl.value !== maxEl.value){
          curEl.value = maxEl.value || '0';
          curEl.dispatchEvent(new Event('change', { bubbles: true }));
          count++;
        }
      });
    });
    return count;
  }

  function longRest(){
    let slotsRestored = 0;
    for(let l=1;l<=9;l++){
      const { used } = _slotFields(l);
      if(used && parseInt(used.value,10) > 0){
        used.value = '0';
        used.dispatchEvent(new Event('change', { bubbles: true }));
        slotsRestored++;
      }
    }
    const featuresRestored = _resetActionUses(['long', 'short']);
    renderSpellbookSlots();
    if(typeof showToast === 'function'){
      showToast(`🌙 Long Rest — all spell slots and limited-use features restored.`);
    }
  }

  function shortRest(){
    let slotsRestored = 0;
    const hasPactMagic = (()=>{ try { return !!(ACTIVE.cls && ACTIVE.cls.pactMagic); } catch(e){ return false; } })();
    if(hasPactMagic){
      for(let l=1;l<=9;l++){
        const { used } = _slotFields(l);
        if(used && parseInt(used.value,10) > 0){
          used.value = '0';
          used.dispatchEvent(new Event('change', { bubbles: true }));
          slotsRestored++;
        }
      }
      renderSpellbookSlots();
    }
    const featuresRestored = _resetActionUses(['short']);
    if(typeof showToast === 'function'){
      const slotMsg = hasPactMagic ? ' Pact Magic slots restored.' : ' (Spell slots don\'t recover on a Short Rest for this class — only on a Long Rest.)';
      showToast(`☀️ Short Rest — limited-use features on a Short Rest recharge restored.${slotMsg}`);
    }
  }

  function wireSpellbookChrome(){
    const openBtn = document.getElementById('btnOpenSpellbook');
    const closeBtn = document.getElementById('btnCloseSpellbook');
    const overlay = document.getElementById('spellbookOverlay');
    if(openBtn) openBtn.addEventListener('click', openSpellbook);
    if(closeBtn) closeBtn.addEventListener('click', closeSpellbook);
    if(overlay) overlay.addEventListener('click', (e)=>{ if(e.target === overlay) closeSpellbook(); });
    document.addEventListener('keydown', (e)=>{ if(e.key === 'Escape') closeSpellbook(); });

    const longBtn = document.getElementById('btnLongRest');
    const shortBtn = document.getElementById('btnShortRest');
    if(longBtn) longBtn.addEventListener('click', longRest);
    if(shortBtn) shortBtn.addEventListener('click', shortRest);

    // keep the sidebar in sync with edits made in the underlying table
    // while it's open (name/level/prepared changes, rows added/removed)
    const body = document.querySelector('#spellsTable tbody');
    if(body){
      body.addEventListener('change', ()=>{ if(overlay.style.display !== 'none') refreshSpellbook(); });
      body.addEventListener('input', ()=>{ if(overlay.style.display !== 'none') refreshSpellbook(); });
      const observer = new MutationObserver(()=>{ if(overlay.style.display !== 'none') refreshSpellbook(); });
      observer.observe(body, { childList: true });
    }
    // and with slot total edits
    for(let l=1;l<=9;l++){
      const totalEl = document.querySelector(`[name="slotTotal_${l}"]`);
      if(totalEl) totalEl.addEventListener('input', ()=>{ if(overlay.style.display !== 'none') renderSpellbookSlots(); });
    }
  }

  function init(){
    ensureModalWired();
    wireSpellbookChrome();
  }

  if(document.readyState === 'loading'){
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }

})();
